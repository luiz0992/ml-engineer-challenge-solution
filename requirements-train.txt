# ---------------------------------------------------------------------------
# Training and model-optimisation stack. Install on top of requirements.txt:
#
#     pip install -r requirements.txt -r requirements-train.txt
#
# Torch is pulled from the CUDA 12.8 index. The target GPU for this project is
# an RTX 5000 Ada (sm_89); cu128 wheels run under CUDA 13.x drivers via forward
# compatibility. For CPU-only machines, drop the --extra-index-url line and
# onnxruntime-gpu.
# ---------------------------------------------------------------------------
--extra-index-url https://download.pytorch.org/whl/cu128

torch>=2.8
torchvision>=0.23
transformers>=4.46
timm>=1.0.11
datasets>=3.1
pycocotools>=2.0.8

scipy>=1.14
scikit-learn>=1.5
pandas>=2.2
matplotlib>=3.9

tqdm>=4.67
requests>=2.32

# GPU execution provider for ONNX Runtime, used by the benchmark harness.
onnxruntime-gpu>=1.20
